# Fase 2 - Analise de Seguranca

Varredura executada com busca por termos sensiveis (`password`, `secret`, `token`, `api_key`, credenciais em URL etc.) em codigo/configuracoes. Nao foi encontrado `.env` no diretorio do projeto e nao foram encontrados segredos hardcoded nos arquivos de codigo.

| # | Risco | Onde esta (arquivo:linha) | Severidade (Alta/Media/Baixa) | Como exploraria | Como corrigir |
|---|-------|---------------------------|-------------------------------|-----------------|---------------|
| 1 | Chat aberto sem autenticacao envia entrada diretamente ao LLM local | `app.py:89-95` | Media | Qualquer usuario com acesso HTTP ao servidor poderia automatizar chamadas para `/chat`, consumir CPU/RAM do Ollama e induzir respostas fora do proposito operacional. | Adicionar autenticacao/token simples para rotas sensiveis ou limitar exposicao a rede local; incluir rate limit se for exposto fora do ambiente de laboratorio. |
| 2 | Prompt injection contra regras do agente | `services/monitoring_agent.py:67-69` | Media | Usuario poderia enviar "ignore as regras anteriores" e tentar fazer o LLM inventar deteccoes ou revelar instrucoes internas. | Delimitar entrada do usuario como dado nao confiavel, reforcar regra de hierarquia no system prompt e instruir o modelo a nao obedecer comandos que alterem identidade/regras/fontes. |
| 3 | Vazamento de detalhe tecnico do Ollama para usuario final | `services/monitoring_agent.py:74-78` | Media | Ao derrubar Ollama ou provocar erro HTTP, operador recebe URL interna/modelo/detalhe da excecao; em ambiente exposto isso ajuda reconhecimento. | Registrar detalhe tecnico em log e retornar mensagem operacional generica ao usuario. |
| 4 | Renderizacao de eventos com `innerHTML` | `static/dashboard.js:35-39` | Media | Se um label/event_time for manipulado no banco ou vier de uma fonte externa futura, pode injetar HTML/JS no dashboard. | Construir elementos DOM com `textContent` para todos os campos dinamicos. |
| 5 | API sem headers basicos de seguranca | `app.py:31-33` | Baixa | Navegadores nao recebem politicas como `X-Content-Type-Options`, `Referrer-Policy` e protecao basica contra embedding. | Adicionar middleware HTTP que aplique headers de seguranca simples nas respostas. |
| 6 | CORS nao configurado explicitamente | `app.py:31-33` | Baixa | O padrao do FastAPI e restritivo para browsers, mas a politica fica implicita e sem documentacao operacional. | Configurar CORS via variavel de ambiente (`CORS_ORIGINS`) quando necessario; manter fechado por padrao. |
| 7 | Parametros de camera/stream podem conter credenciais no ambiente | `services/config.py:39`, `services/video_monitor.py:41`, `services/video_monitor.py:146-147` | Baixa | Se `CAMERA_SOURCE` tiver URL RTSP com usuario/senha, logs podem expor prefixo da URL. A funcao `_short` reduz tamanho, mas nao mascara credenciais. | Mascarar credenciais antes de logar fontes HTTP/RTSP. |
| 8 | Entrada do chat tem validacao limitada | `services/schemas.py:29-30`, `app.py:91-94` | Baixa | Payload ainda pode conter controle textual hostil ao LLM, embora tamanho seja limitado a 2000 caracteres e vazio seja bloqueado. | Manter limite Pydantic, normalizar a mensagem e adicionar orientacao anti-injecao no prompt; rate limit/autenticacao se houver exposicao real. |
| 9 | Injecao SQL nos eventos | `services/event_repository.py:49-52`, `services/event_repository.py:59-62` | Baixa | Nao ha exploracao direta observada: consultas usam parametros vinculados, inclusive `LIMIT ?`. | Manter padrao parametrizado nas novas tabelas; nao concatenar entrada externa em SQL. |
| 10 | Scraping/API externa futura pode contaminar banco/prompt | Ainda nao implementado nesta fase | Media | Conteudo externo indisponivel, malformado ou manipulado poderia ser salvo como confiavel e repassado ao LLM sem contexto, causando respostas erradas. | Usar cliente HTTP com timeout/retry, validar com Pydantic, salvar snapshot estruturado, sanitizar campos textuais e informar fonte/timestamp ao agente. |
| 11 | Upload de arquivos | Nao ha rota de upload implementada | Baixa | Nao aplicavel no codigo atual. | Se upload for adicionado, validar tipo, tamanho, extensao e armazenar fora de caminhos executaveis/publicos quando possivel. |
| 12 | Rotas de escrita no banco | `services/video_monitor.py:131-135`, `app.py:18-21` | Baixa | Nao ha endpoint publico que escreva eventos diretamente; escrita ocorre no processo interno de monitoramento ao detectar objetos. | Manter escrita de eventos isolada no servico; para futuras rotas de administracao, exigir autenticacao. |

